# Research archive architecture

This repository preserves a checkable mathematical programme with small, independently runnable experiments. It does not implement the Alpha-devbox agent controller.

* `RESEARCH_STATE.md`: the current objective, assignments and synchronization state.
* `research/RESEARCH_LOG.md`: chronological questions, results, failures and decisions supported by evidence.
* `research/claims/CLAIM_LEDGER.md`: authoritative claim scope and proof status.
* `research/reports/`: complete mathematical arguments and independent reviews.
* `research/dyson/round7/`: actual-zeta two-scale and compact Fourier reductions, the fixed large-prime arithmetic mark, and deterministic-flow/DBM obstructions. Each folder owns its derivations, scalar or finite checks and reviews. The intake manifest distinguishes original source bytes from three publication edits; `research/logs/round7-integration/` replays the bounded experiments in a temporary copy. Full third-party papers stay in the adjacent local reference archive.
* `research/dyson/round8/`: the actual-zeta short-prime projection and centered-error remainder, plus a fixed positivity minorant and its realized obstruction. All received files are verbatim; independent analytic reviews sit beside their author reports. `research/logs/round8-integration/` replays the two small scripts and verifies the cutoff convention with exact rational finite measures.
* `research/dyson/round9/`: the complementary-modulus Type I transfer, a fixed two-large-prime arithmetic interaction, the quantified prime-power tail estimate, and the mesoscopic edge source/rate audit. Author files and subsequent reviews are verbatim. The intake manifest identifies locally retained primary references; `research/logs/round9-integration/` runs one q32 calculation and three check scripts in a temporary copy.
* `research/dyson/round25/`: actual joint-main cancellation over the full compact height range, an independent odd-Mobius input, complete author/Plato/coordinator reviews, and the exact Fourier covariance with its root source/proof audit. Original primary bodies remain local with hashes. `research/logs/round25-integration/` checks both whole replay pairs and immutable source bytes.
* `research/dyson/round24/`: unrestricted actual nonprimitive removal, exact small-cofactor centers and their analytic obstruction, and one/two-profile plus finite compact-window zero-margin impossibility proofs. Complete author and independent/coordinator records remain verbatim. Late R23 evidence and the revised source-tools memo are separate files. `research/logs/round24-integration/` verifies hashes and prior bounded replay bytes.
* `research/dyson/round23/`: actual upper-window physical-shift completion, fixed-six pair normalization and growing-wheel residual/heat comparison. Complete author and independent proofs, a copied scalar replay and the coordinator second-reading memo remain verbatim. Late R22 singleton/publication reviews are preserved as new evidence without rewriting historical status. `research/logs/round23-integration/` checks all bytes and exact replay outputs.
* `research/dyson/round22/`: uniform small-shift removal, complete singleton renormalization, all-odd prime-power pairs and parity-adjusted target. Distinct author proofs, an independent full derivation, cross-reviews and root assembly keep the exact coefficient/assumption changes visible. Source receipts, copied scalar outputs and integration hashes support reproduction; no new data scan or PDF build.
* `research/dyson/round21/`: exact all-length finite arithmetic kernel, fixed-bump Wiener theorem, actual log-prime heat energy, and the centered pair remainder. Author proofs, independent/root/coordinator reviews, original superseded feasibility claims and the h=1 correction remain distinct and pinned. Integration verifies preserved source bytes and bounded replay outputs; primary source bodies remain local with public hashes.
* `research/dyson/round20/`: actual exponential length averaging, height regularity and their quantitative strict-deficit equivalence under RH. A separate fixed three-height computation retains every per-bin CSV row and integer prime-power array. Author reports, cross-reviews, root/coordinator reviews, historical memo states and exact symbolic replays remain verbatim. `research/logs/round20-integration/` verifies source hashes, CSV totals, array bytes and strict math syntax; no numerical sweep is repeated for publication.
* `research/dyson/round19/`: a positive centered prime variance and conditional deficit theorem; a uniform finite ACUE heat theorem and coherent-mode curvature; and an exact physical-shift dispersion audit. Original proofs, independent/root/coordinator reviews, historical revisions and matching bounded replays remain separate. The intake manifest and source-link map locate all original dependencies; `research/logs/round19-integration/` verifies hashes and math syntax without another mathematical rerun.
* `research/dyson/round18/`: functional reflection, a carrier-one trace and its actual large-carrier divergence; dense-modulus support thresholds, completed coefficient norm and exact progression normalization. Full author/independent/root/coordinator records remain verbatim, including historical review snapshots and the final delta. A copied exact-algebra replay and explicit documentation-only metadata comparison sit beside the reviews; `research/logs/round18-integration/` pins the intake.
* `research/dyson/round17/`: the actual centered short-interval cap calculation and its scale failure, plus compact holomorphic-square/positive-energy identities with reflected residues and weight derivatives. Full proof reviews and bounded exact checks are retained; `research/logs/round17-integration/` records unchanged replays.
* `research/dyson/round16/`: the actual frequency-two AH saturation/atom-capacity theorem, its full centered prime kernel and a separate compact pole-annihilating packet, with independent and root reviews. Three bounded outputs replay verbatim in `research/logs/round16-integration/`. Full primary reference bodies and exact duplicate replay inputs stay local with public hash receipts.
* `research/dyson/round15/`: exact Vaughan remainder and untwisted coefficient distribution, plus a pole-annihilating Gaussian packet for actual zeta, with author, independent and root reviews. Its intake manifest retains all originals locally and identifies two duplicate replay inputs omitted publicly. Complete bounded JSON replays are in `research/logs/round15-integration/`.
* `research/dyson/round14/`: actual smooth Type I removal and selected-minimum CUE background/quantitative scalar-heat proofs, with independent reviews. `research/logs/round14-integration/` checks both finite scripts and inherited proof/source hashes, including the prior attribution-only publication edit.
* `research/dyson/round13/`: exact rational-core extraction, signed CRT norm remainder and primary minor-arc source audit, with root and independent author reviews. Two bounded algebra scripts replay under `research/logs/round13-integration/`; full reference bodies stay local.
* `research/dyson/round12/`: actual-support sampling obstruction, two explicit dispersion-hypothesis failures, and a centered Selberg precision audit. The complete root review pins all three authors; exact replay in `research/logs/round12-integration/` includes 60 signed-kernel identities. No additional prime estimate is claimed.
* `research/dyson/round11/`: the RH small-arc improvement, actual-prime conductor-mass lower bound and centered mixed-tail arithmetic identity, each with independent review. The originals are verbatim; two exact scripts replay under `research/logs/round11-integration/`, with only a temporary source path excluded from one certificate comparison.
* `research/dyson/round10/`: the actual smooth shift-completion bound, two independent component reviews, and the source-backed prime-variance/mixed-moment audit. Two exact scripts are replayed under `research/logs/round10-integration/`. This slice adds an analytic bound, without a numerical search or new infrastructure.
* `research/prime-gaps/round4/`: outward lower-credit integrals, independent kernel/projection audits, k=39 cap matrices and coefficients, and the isolated corrected arithmetic-runtime record. Its external primary source paths are configurable; the full third-party numerical paper text is not duplicated.
* `research/prime-gaps/round5/`: variable-radius exact exceptional constants, source/cap admissibility and mesh-repair proofs, and the bounded negative geometry search with complete finite matrices. The independent integration replay is under `research/logs/round5-integration/`.
* `research/prime-gaps/round6/`: the full signed cap operator, independent nonnested-projection audit, one radial residual direction, exact outside-span certificate and four compact numerical witnesses. `research/logs/round6-integration/` preserves the independent fine replay and archive checks. The four full density-cache archives remain in the adjacent local archive, identified by public hashes; no candidate arrays are removed from the compact witnesses.
* `research/residual-gram/`, `operator-bounds/`, `heat-flow/`, `centered-gaussian/`, `prime-gaps/`, `dynamic-generator/`, `force-energy/`, `bridge-audit/`: focused Python experiments with JSON outputs and original execution logs.
* `fable/overnight-2026-09-05/` and `fable/snapshots/89393d5/files/`: separate byte-for-byte public Fable snapshots with their own provenance manifests. `fable/reviews/pr11-89393d5/` holds the later arithmetic and background objections, an independent exact pole check and a bounded replay that never alters source bytes.
* `fable/snapshots/2073028/files/` and `fable/reviews/pr11-2073028/`: the repaired 160-file source receipt, separate F1/F3/general-beta audits, a mass-weighted Fock boundedness proof and a deliberately small finite-model check. Source bytes are never rewritten by corrections.
* `historical/riemann-rmt/`: pinned earlier public sources, preserved even when corrected by later audit.
* `docs/handoff/`: the earlier detailed archive, separately pinned Rounds 4–5 and Rounds 6–14 supplements, and the complete public Markdown with a per-volume hash/page index.
* `output/pdf/`: the public rendered volumes and the 705-page complete compilation. The private 753-page compilation remains adjacent to the repository in `Astra-Local-Archive`.
* `tools/`: manifest validation and document assembly/rendering.
  `build_round45_handoff.py` and `build_round6_14_handoff.py` assemble complete reports against fixed source commits. The latter records every equivalent inline-math table repair without editing source files. `render_handoff.cjs` renders the volumes; `build_complete_handoff.py` combines them and checks all extracted text and page offsets. `inspect_pdf_pages.py` and `check_merged_pdf.py` retain mechanical and raster evidence for visual review; final receipts and the independent coverage checker are in `research/logs/round6-14-handoff/`.
* `tasks/`: one bounded task for the user's existing Fable session.

The adjacent local folder `Astra-Local-Archive` contains user-supplied full context and optional large data. It is separate from Git. New mathematical reports, important results, scripts and hashes belong here; credentials and unrelated private conversations do not.

Python handles arithmetic and experiment orchestration; JavaScript handles Markdown/KaTeX PDF rendering. No Rust kernel is needed for this slice. A test of a computation is evidence about that computation, not an automatic formalization of its mathematical theorem.

The portable acceptance surface is: verify hashes; run the bounded algebra/certificate checks in a copy; rebuild the handoff; inspect PDF math and layout. Full eigenvalue sweeps are optional and explicitly sized. No dashboard, distributed orchestration, model-calling service or new paid session is introduced.
