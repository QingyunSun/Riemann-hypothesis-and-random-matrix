# Root review of the weighted Selberg-variance target

Date: 2026-09-05. Root read the entire frozen variance author, SHA256 `0c5323ac5a983148a9ec433ea1196fb0fd538f00872ac73e9de3ae105c7a2502`, 21,201 bytes, and its complete exact checker. I accept the ordinary RH-dependent Abelian reduction, conditional quantitative implication and exact finite arithmetic kernel. The separate Euclid review supplies another independent audit and a bounded copied replay. No strict variance bound is accepted as proved.

I checked the primary CCCC PDF, printed page 25, equation (3.8), the uniform prefix equality immediately before (3.9), and Lemma 13. The source has the exact factor (2/pi)*log^2(T) before the time integral and error O_g(1/T). Multiplication by T/log^2(T) gives the author's 2T/pi and O_g(log^(-2)(T)). The prefix identity has two squared zero sums and factor 2Y, uniformly when Y is between T/log^3(T) and T*log^3(T). The source's extra plateau conditions are used for interval comparisons, not the underlying identity. The author uses the latter correctly.

With t=Ty, the rescaled measure is d(K_T(Ty)/T). The factor outside the limiting kernel is 2/pi, and k(y)=sin^2(y/2)/y^2 integrates to pi/4 on the positive half-line. A full limit C_Y->A therefore gives precisely A for the positive prime variance. The two far height ranges contribute O_g(1/log T) by the source pointwise logarithmic envelope. Inside that range, the prefix mass is O_g(y); its small interval and large tail contribute O_g(eta) and O_g(1/R). These estimates justify the successive compact-limit and tail removal steps, including replacement of T*kappa_T by 1/2. No weak convergence over an uncontrolled infinite interval is used.

The square-root approximation is needed because the exact autocorrelation bump was not independently shown to have a smooth compactly supported square root. The proof correctly fixes one smooth-square approximation before taking height limits. Positivity and the RH bounded masses control the uniform approximation error afterward. Schwartz seminorms are not asserted uniform. The same ordering supports the finite-R integration-by-parts argument, without postulating an original Schwartz square root.

The qualitative implication is logically stronger than only restating AH's prediction. R16 gives C_T=A-D_T+o(1) with D_T>=0 under RH. If limsup D_T=0, then D_T->0 and hence C_T has the full limit A; the Abelian argument forces V_T->A. Contraposition shows that liminf V_T<A already entails positive limsup D_T. No common choice of subsequence for the two statistics is required, and the result does not assert their equality at the same height.

For the quantitative estimate, positivity permits retaining only y<=R in the time integral. Integration by parts gives the boundary R*k(R)*C_(TR) and the integral of -y*k'(y)*C_(Ty). Substitution of A-D and the bound D<=d+o(1), away from a vanishing small-y interval, gives the stated tail plus d times a finite total variation. I checked y*k'(y)=sin(y)/(2y)-(1-cos(y))/y^2. The tail is at most 1/R and the integral of |y*k'| to R is at most 3+(log R)/2. The displayed choice R>=8A/(pi*delta) leaves delta/2 and yields the claimed positive lower bound on d. Sending R to infinity inside an absolute derivative integral would be invalid; the proof never does so.

I independently expanded the arithmetic square. An integer n belongs to the short interval exactly when x lies between n/(1+1/T) and n, up to measure-zero endpoints. Intersecting two such intervals produces the nonnegative finite pair kernel. The cross term has coefficient -2/log^2(T) times the integral dx/x, and the continuous square has coefficient 1/(T*log^2(T)) times the integral dx. All prime powers remain. The diagonal asymptotic uses the full Lambda(n)^2 sum and the exact unweighted cell integral 1/(Tn); its limit is integral alpha*omega(alpha)=2*epsilon*m0. The remaining centered combination is signed even though the pair kernel is nonnegative.

For the optional localized sufficient premise, a log-x cell of length eta has mass at most (B*eta+o(1))*log(T)/T uniformly in its center. There are O(log T) cells. The exterior T/log^2(T) normalization leaves a bounded upper Riemann sum, and the uniform cell errors vanish at each fixed eta. This gives B*epsilon*m0. Without uniformity in the cell center or without the single-log scale, the inference would fail. The author explicitly labels this premise unproved and distinguishes it from an additive fixed-h statement needing a separate conversion.

The literature table is a bounded range audit. The long-frequency-window constants cannot be multiplied by the width of this fixed narrow bump; extra pair-correlation hypotheses cannot be replaced by RH. The coordinator separately checked the new-paper range guards, which are retained in the intake. CCCC's monotone-envelope upper transfer gives L^+*A, exceeding the AH target. The exact full-limit transfer removes that loss only after a full limit is supplied; it does not create a new arithmetic inequality.

Accepted conclusion: an explicit positive, fully centered short-prime-interval statistic whose strict subsequential deficit would refute AH-Pairs under RH, with a quantitative deficit conversion and a reproducible finite kernel. The actual strict estimate, any actual-zero numerical conclusion, and all famous-conjecture claims remain open.
