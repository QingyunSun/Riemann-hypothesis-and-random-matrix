# Root review of the frequency-two saturation target

Date: 2026-09-05. Reviewed the entire frozen author proof `BRAGG_ATOM_TARGET.md`, SHA256 `2228bfd90e7a633683936d3d611f31c1f960107fbdf111a494993f73be16e120`. This is an ordinary mathematical review, not proof-assistant verification or an independent priority assessment.

I accept the stated exact comparison and the RH-dependent limit reductions. The finite pair measure is positive and even; the separate Lorentzian positive-definiteness argument makes F nonnegative. The autocorrelation bump is nonnegative in both variables and satisfies psi(0)=1. Under the specified Fourier convention, the test has one factor epsilon after Fourier inversion and no inverse-epsilon normalization on the spectral side. The deficit is exactly epsilon times the integral of hat psi(epsilon*u)(1-cos(4pi*u)) against the pair measure. Its sign and its vanishing on the half-lattice follow term by term.

The low-band limit includes an atom of mass one at zero, not only the density |alpha|. It therefore yields 1+epsilon²*m1. Compactness of spectral measures follows by translates of one positive bump; atom capacity at any fixed location is at most one by taking the subsequential limit before epsilon decreases to zero. This capacity alone does not exclude a nonzero atom.

For the AH implication, the author uses the required separate local mass and large-distance tail bounds. The linear pair-mass bound is used only through R<=T; the remaining total mass gives the stated log(T)/T tail. Early-zero removal is O(1/log T) on fixed normalized compact sets. The resulting pair limit is supported on the half-lattice, so its Fourier distribution has period two. Translating the known open band gives the atom and density inside (1,3), without fixing p0, simplicity, or endpoint atoms at one and three. This proves full-sequence saturation for fixed epsilon<1. The positive-density geometric implication is about pairs, not consecutive gaps. A converse cannot ignore the Fourier zeros of the bump.

The source comparison keeps the finite interval and long-average statements separate. The rational value 1085/768 is one admissible CCCC candidate, not an assertion that it is their minimum. The tailored bump bound is an application of the same positivity argument, not a new interval-optimization record. The 1.3208 long-average constant cannot be multiplied by an arbitrary short length.

I checked the extended actual prime representation against its displayed explicit-formula estimates and inherited R7 normalization. RH makes the zero phases unit modulus for every positive x. The source errors after dividing by x*T*log T are O(log T/x+log²T/T); they are uniform on any fixed positive compact alpha range. The full complex continuous mean, all prime powers, and both centered cross terms remain. The atomic diagonal is integral alpha*psi_epsilon(alpha)dalpha=2*epsilon*m0. Thus the centered off-diagonal upper target and both AH/sine benchmarks have the displayed constants. At epsilon=1/4 the natural x/T exponents are [3/7,5/9]; the previously discarded O(H*sqrt X*log^4 X) cannot be certified negligible there. This is failure of that estimate, not an actual-error lower bound.

No strict actual-zeta deficit, new pair-correlation limit, AH refutation or famous conjecture follows. The author has isolated an exact sufficient target and retained the arithmetic terms that any successful estimate must control. The bounded replay checks finite normalization and constants only; it does not prove the asymptotic source inputs or the missing inequality.
