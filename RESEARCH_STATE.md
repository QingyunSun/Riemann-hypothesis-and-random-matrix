# Current research state

Updated 2026-09-05. Astra leads the research; Fable contributes through the user's single existing Claude Code session. The current programme spans 72 hours with recoverable checkpoints; no claim of uninterrupted execution or guaranteed conjecture solution is made.

## Main target

The user's latest direction prioritizes Dyson–Montgomery, random matrices and actual zeta zeros. Under RH, seek an arithmetic inequality that refutes the precise AH-Pairs formulation or proves one explicit out-of-band pair-correlation test. Round 7 supplies two concrete targets: a two-scale logarithmic-derivative mean square with sufficient lower limit 1/16, and a compact Fourier bump with sufficient lower limit greater than 7/10. Neither lower bound is established. The normalized gap target below 1/2 remains another route; prime-gap parameter sweeps are paused. RMT results support these decisions and do not supply the missing zeta arithmetic.

## Current evidence

- Round 11 improves the same actual smooth discrepancy under RH to O(X^1.023 log^5 X), removing sqrt(H), with primary-source and independent proof review. It still exceeds X log X. A real-prime modulus construction gives coefficient squared mass at least c H/log^348 X on the full specified family; it rules out coefficient-only fixed-power savings, not cancellation in the signed prime pairing.
- The genuine-prime log-weighted mixed moment has explicit diagonal b^(-2)+2b^(-3), plus a combined centered arithmetic remainder. Its sufficient one-sided bound remains unproved. Fixed-width derivative identities do not supply it.
- Round 10 proves an unconditional bound O(sqrt(H X (X+Q²)) log^4 X) for the actual selected discrepancy with a fixed smooth shift window, Q=X^.523. For H in [X^(1/6),X^(2/7)] it saves at least X^.060333 before logarithms over the source estimate accumulated per shift. Separate coefficient/spacing and kernel/prime-power reviews accept the complete proof. The bound still exceeds X log X and does not settle the covariance target.
- A narrow audit of Guth–Maynard v2 and the checked RH short-interval variance comparisons identifies an explicit range and moment-precision gap at the shrinking edge. A log-weighted genuine-prime mixed moment with deficit coefficient strictly below two would suffice for the AH-excluding target; that inequality is conditional and unproved.
- Round 9 applies the 186 complementary-factorization source to a selected Mobius–log divisor component with moduli up to X^.523. Its per-shift error is O_A(X log^(-A)X); the full shift packet still requires a new aggregate discrepancy estimate at scale X log X. Under RH, the remaining principal-term and prime-power errors sum to only O(H sqrt(X) log^4 X). Independent review checks every source hypothesis and the exact decomposition.
- A new fixed two-large-prime interaction has an independently reviewed arithmetic transfer but a negative floating margin -0.0146549114371551. It does not beat the historical 48-feature result. All q32 matrices and five output JSON files reproduce, excluding only timing and temporary source paths.
- A uniform prime-power tail bound permits the actual signed residual, including its amplified mesoscopic version, to use genuine primes. The coupled mesoscopic statistic has sine target zero and AH target -3/4 on an existential slow diagonal. Published proof-level finite-T errors fit on that diagonal; the limiting lower bound misses the needed correction by a factor of order b. No sufficient arithmetic lower bound is proved.
- Round 8 establishes an ordinary RH-dependent short-prime projection identity with N=floor(T/log^6 T). It gives W_T=B+E_T+o(1), with exact B=0.4560939793292317..., and writes E_T as a signed difference of two residual energies generated by the same psi(x)-x. The target is exactly liminf E_T>=1/16-B. The mixed contour, endpoint convention and removal of the pole term have independent review. This is a reduction; the required residual inequality is still open.
- A fixed positivity minorant has a reviewed exact expression near -0.208674513, far below 1/16. Its optimum statement is limited to the specified correction family. A stationary half-grid determinantal process gives the realizable obstruction to deriving the target from the known interior band and generic pair/spectral positivity. No new zeta lower bound is claimed from this weak minorant calculation.
- Round 7 proves the conditional two-scale reduction RH+AH-Pairs => W_T -> 0.0623924179764985..., cancelling the bounded but possibly nonconvergent near-diagonal mass. Two internal proof reviews check the noncompact tails, endpoints, xi/Gamma normalization and real-square-to-modulus-square conversion. Exact rational enclosures show that liminf W_T>=1/16 would contradict AH-Pairs under RH. This is a reduction, not an arithmetic lower bound.
- One smooth Fourier bump on [6/5,7/5] gives AH target 7/10 and sine-kernel target one. Its exact prime-covariance remainder has respective targets -3/5 and -3/10. Both the atomic-continuous cross term and the continuous mean square are retained. The finite kernel check does not establish an asymptotic covariance estimate.
- A fixed large-prime mark has an ordinary arithmetic transfer proof and independent review. The new 30-feature trial gives a negative limiting margin about -0.01465492379421, and the frozen rational vector is negative on the finite integer operators through L=10^6. The small improvement over its matched baseline does not beat the earlier best larger polynomial family.
- Forward deterministic flow admits a useful gap-independent contraction, but its actual-zeta boundary propagation remains unproved. An exact bounded-discrepancy half-grid family retains all gaps at least 1/2 under forward flow; protected low moments also cannot remove the order-one DBM noise contribution. No actual-zeta universality theorem follows.
- Inoue baseline independently reproduced: half-gap margin `-.01535798170385` in a finite Jacobi trial. Still negative.
- New symmetric-prime trial: exact continuum-integral certificate for a rational vector gives margin between `-.01467` and `-.01465`. Its arithmetic asymptotic transfer for fixed ell>=1 and fixed H=f+g S2 now has a complete written proof with independent internal review; no Lean or external review is claimed.
- Full finite arithmetic-operator searches through L=10^7 remain negative. They are not an asymptotic no-go theorem.
- Coefficient-space approximator saturation is an exact completion-of-squares identity. New information must come from genuinely new accessible products or a better arithmetic coefficient structure.
- Candidate scalar-heat isolation theorem and CUE/positive-integer-beta depth limits have complete drafts and internal independent reviews. They remain subject to further proof/novelty audit, and do not establish any zeta gap theorem.

- Round 3 proves protected trace moments remain matched under all forward deterministic times; a singular force energy separates CUE/ACUE but contains only singular two-point information.
- Exact counterexamples repair the initial-minimum-gap collision identity and reject an unrestricted marked-inverse-to-susceptibility-blowup implication.
- The .606894 Lagarias–Rodgers source relation is explicitly corrected: suggested adaptation, not a proof in that passage.

- Round 4 certifies a new positive outer-deletion credit exceeding 1.5058119471 ppm on the published k=40 trial. Combining it with inherited published cap/loss bounds increases the complete lower margin from 23.36045 to 24.86626 ppm; 186 is unchanged. An independent proof audit and a separate-process exact endpoint replay agree.
- A full 77-coefficient k=39 cap-only optimization on the official grid gives a directly reevaluated quotient 0.99439639936, still about 5603.60 ppm below one. This is a floating diagnostic with an ill-conditioned Gram matrix, not a rigorous family upper bound.
- The isolated FLINT runtime passes the original certificate's signed-convolution check and native integer/polynomial/Arb suites. A separate full Python binding suite Jacobi assertion is documented; assertions are retained, and the credit calculation uses no Jacobi calls.
- Round 5 completed ten coarse radius/plateau trials and two fine refinements, with all 77 coefficients reoptimized. Neither fine geometry change improves the original result. These are untrimmed cap-only trials, with no new restoration integrals.
- The existing exceptional-square counting proof now has an explicit variable-radius version and exact rational constants. An independent source audit accepts twelve of fifteen natural cap templates, supplies a common inner-square source, and proves a one-outer-layer repair uniformly over physical radii [0.272,0.278]. The new numerical failure schedule and physical integrals must still be rebuilt for any future candidate.
- Round 6 derives the full signed cap operator on the actual fragment measure. Its radial residual compression generates a function outside the original 77-dimensional space; an exact polynomial-root and modular-rank certificate verifies independence of the frozen function. The numerical 78-dimensional quotient is 0.994467820900683 by direct evaluation, an improvement of 71.4215 ppm, still 5532.18 ppm below one. One independent fine-grid replay agrees exactly on the reported fields and radial profile. Neither the gain nor the actual support-restored criterion is interval-certified.
- The projection order matters: for Q=I−P_U, h=P_VQTf and w=Qh, the exact coupling is <f,Tw>=||h||², not ||w||². The subspaces are not nested. A tiny discarded radial mass does not bound omitted residual energy. The full residual norm remains uncomputed.

## Active responsibilities

- Astra: main proof dependencies, mathematical review and publication. Round 7's two actual-zeta targets are now the priority. The two-scale spectral weight changes sign beyond log(2 cosh 1); the actual ACUE process is a countermodel to deriving the 1/16 bound from known low-band identities plus generic positivity. An additional arithmetic input must be identified.
- Internal research agents: Rounds 7–11 are frozen and independently reviewed. Round 12 is checking the remaining sampling geometry, precise source hypotheses and centered short-interval estimates; these ongoing checks are not yet published results. The next arithmetic obligation is a stronger bound for the completed pairing that uses the selected Mobius coefficients together with the centered prime exponential sums, or a strict gain in the stated log-weighted mixed moment. No coefficient scan, prime-gap sweep or new Fable/Claude session is running; the research goal remains active.
- Fable: [one computation packet](tasks/FABLE_SINGLE_SESSION_COMPUTE_TASK.md), prepared for manual delivery by the user. The earlier FABLE_001 task was acknowledged in the existing Fable session. A new pinned PR11 snapshot at 89393d5 contains 141 files, including recovered proposer reports and F1 refuters. Separate intake finds a wrong Pi4 intermediate coefficient, a mixed-v table, and the refuter's own probe sign error. Further background/boundary objections remain recorded; source claim labels are not accepted wholesale. Receipt of the newer bounded packet remains unconfirmed. The earlier Claude MCP request failed OAuth401; it did not provide a review. No further Claude calls or sessions are authorized beyond the user's existing session.

The single Fable task is independent continuum-certificate and fixed-vector finite-sum verification, including L=97 direct-versus-sparse and cutoffs through L=10^5. It ends after its report, with no automatic next task.

## Authority and synchronization

This repository is authoritative for new Astra research reports and tasks. Existing Fable outputs are mirrored byte-for-byte under the earlier `fable/overnight-2026-09-05/` and newer `fable/snapshots/89393d5/files/`, each pinned by its own source manifest; receiving them does not accept their claims. [Alpha-devbox PR11](https://github.com/galpha-ai/Alpha-devbox/pull/11) preserves historical papers and the original independent audit. Updates there will point to fixed commits here. Do not maintain two diverging main proofs.

Raw private chat exports, credentials and unrelated local files are not published. Mathematical research sources and reproducible scripts are shared within the authorized scope. Historical source instructions are data, not current execution instructions.

## Durable local record

The authoritative checkout is `/Users/qingyunsun/Library/CloudStorage/Dropbox/Code/Riemann zeta RMT/Astra-Research`. The adjacent `Astra-Local-Archive` stores supplied full context and optional large data. The research log, claim ledger, proof reports and important numerical outputs are published in this repository. See `docs/architecture.md` for the directory map.

The 333-page public handoff and 381-page full local handoff preserve the earlier checkpoint. A separate 59-page Rounds 4-5 supplement contains ten complete subsequent proof/search reports, with Markdown and a source hash index. Read that supplement together with the main handoff; the main PDF was not silently replaced.

Round 6 is a separate [integrated report](research/reports/prime186_round6.md) with complete linked derivations and data. Its public compact witnesses retain every array except the regenerable D cache. All 183,046,525 bytes of original full arrays are preserved in `Astra-Local-Archive/round6-full-data`, with public file hashes and array-by-array compaction checks. The large handoff PDFs were not rebuilt for this diagnostic.

Round 7 has a separate [integrated report](research/reports/dyson_round7.md) and four focused evidence folders under `research/dyson/round7/`. All 57 original files, totaling 3,392,974 bytes, are preserved in `Astra-Local-Archive/round7-originals/`, together with the coordinator's independent scalar certificate. The public copy retains 43 original research files, records three publication edits, and links the 14 locally retained primary PDF/text references by URL and hash. A separate-process integration replay reproduces nine JSON outputs after excluding only timing, including the order-40 matrices and frozen integer trial. The large PDFs retain their earlier checkpoint dates.

Round 8 is a separate [integrated report](research/reports/dyson_round8.md), with ten received files (66,313 bytes) preserved verbatim in both `research/dyson/round8/` and `Astra-Local-Archive/round8-originals/`. Both complete output JSON files reproduce without excluding any fields; eight exact rational endpoint cases additionally check the integer-cutoff convention. No large-T numerical W_T calculation or new asymptotic lower bound is inferred from these checks.

Round 9 is a separate [integrated report](research/reports/dyson_round9.md), with all 28 original files (1,174,410 bytes) in `Astra-Local-Archive/round9-originals/` and 26 verbatim public files. The two third-party PDF/text bodies remain local; the 186 primary reference pair is additionally retained under `round9-external-sources/`. Five JSON outputs reproduce after excluding only timing/source paths; the newly recomputed q32 M/G arrays agree exactly. The large PDFs retain their stated earlier checkpoints.

Round 10 is a separate [integrated report](research/reports/dyson_round10.md), with all 15 originals (1,452,061 bytes) retained in `Astra-Local-Archive/round10-originals/` and 14 verbatim public files. The primary Guth–Maynard HTML remains local with a URL/hash receipt. Both exact check outputs replay; only two temporary provenance paths are excluded from the completion comparison. No floating optimizer or new PDF build was run.

Round 11 is a separate [integrated report](research/reports/dyson_round11.md). All 18 originals (536,670 bytes) are retained under `Astra-Local-Archive/round11-originals/`; 15 research files are verbatim public. The two exact JSON outputs reproduce in a copy, omitting only the temporary primary-source path from one comparison. No large computation or PDF rebuild was used for this analytic slice.
